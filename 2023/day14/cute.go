package main

// func score(line string) int {
// 	score := 0
// 	parts := strings.Split(line, "#")
// 	offset := 0
// 	for i := 0; i < len(parts); i++ {
// 		count := strings.Count(parts[i], "O")
// 		for j := 0; j < count; j++ {
// 			score += len(line) - offset - j
// 		}
// 		offset += len(parts[i]) + 1
// 	}
// 	return score
// }
